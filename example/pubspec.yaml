name: example
description: Example app for animated_vector
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  animated_vector:
    path: ../packages/animated_vector

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  lint: ^2.0.2

flutter:
  uses-material-design: true
